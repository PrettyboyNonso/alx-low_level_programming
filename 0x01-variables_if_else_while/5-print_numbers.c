#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: zero to nine
 *
 * Return: 0 (Success)
 */

int main(void)
{
int x;

x = 0;
for (x = 0; x <= 9; x++)
{
printf("%d", x);
}
printf("\n");
return (0);
}
