#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argc: param
 * @argv: param
 * Return: always 0
 */
int main (int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
