#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argv: param
 * @args: param
 * Return: always 0
 */
int main (int args, char *argv[])
{
(void)argv;
printf("%d\n", args - 1);
return (0);
}
